#include<stdio.h>
#include<stdlib.h>

char *getAddResult(char *inputA ,char *inputB)
{
  //:TODO check input
  //
    unsigned int lenA = 0;
    lenA = strlen(inputA);
    
	unsigned int lenB = 0;
    lenB = strlen(inputB);
	unsigned int len = lenA > lenB ? lenA :lenB;
	while(len >10 )
	{
		a[] = atoi(inputA+lenA-10);
	}
	
	// get 10 len  convert to int add1
	// len -10
	//
	// and get anthon 10 len add2 
	// len -10
	//
	// put the addX into the right loacationg
	// and then add the AddX to get the reslut
	//
	// untl the len < 10
}
